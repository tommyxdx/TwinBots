# 验证记录

## 2026-09-14 审查修订复验

通过 GitHub 插件读取固定提交后，先复现原 41 项测试：全部通过。
修复后的 Windows / Python 3.13.0 环境使用 requirements-tested.txt 所列直接依赖，**62 项测试全部通过（8.432 秒，新增 21 项回归）**。另外已通过离线 `demo` 和 `doctor`；doctor 确认 paper 模式、DEX/Telegram 关闭、安全适配器和远程数据集未配置。额外回归确认禁用启动下载后，小时维护不会立即覆盖该设置。
新增回归覆盖：重定向不泄露凭证、最新安全扫描优先、集中度未知/失败阻断、历史过期与配置变更导致模型停用、仅训练段选择特征、部分卖出估值、持续止损意图、实际费率/滑点仓位预算、DEX 大整数精度与压力成本、非法数据拒绝、保留长期净值路径。

测试均为离线合成夹具和本机 HTTP，不是盈利证据。此次未启动持续市场采集、未调用交易所账户/Jupiter 私有接口、未发送 Telegram、未签名或广播交易。公共供应商文档核对及 API 指引见 API_SETUP_ZH.md。

以下是仓库原作者的历史验证记录，保留以区分本次复验与此前公网结果。


验证日期：2026-09-13。范围是软件行为与接口接入，不是收益验证。

## 已通过

命令：`python -m unittest discover -s tests -v`

**41 项测试通过。**覆盖：

- 秒/毫秒/微秒历史时间戳、未完成 K 线排除、数据重复导入、SHA256 校验及损坏缓存拒绝。
- HTTP 日预算共享、HTTPS 约束、单实例进程锁。
- 订单簿序列桥接/断档、过期行情、可见深度不足、部分成交、同一深度不重复消费、压力价格仍须满足限价。
- 现金/费用、不能借款或卖空、重复结算拒绝、未知回执恢复、未结算订单重启取消、回撤暂停持久化。
- DEX 成功买卖后的整数库存、滑点超限整笔回滚及 gas、掉单不扣费、报价失败不虚构成交、不可报价库存保留。
- 特征不读未来数据、时间缺口不视为连续行情、未来标签排除、训练/校准窗口 purge、相同 token 不跨分段泄露。
- 样本不足不输出概率、安全未知不当成通过、已知安全失败阻断、Telegram 去重。
- 本地真实 HTTP 服务串联：新池发现 → 历史 OHLCV → 安全 JSON → 评分 → SQLite 写入；generic quote 解析；CEX 引擎在等待期间盘口改变/过期时正确拒绝成交。
- 全离线 demo 和 HTML/JSON 报告生成。

这里的本地行情数据全部是合成夹具，不能证明策略盈利。Windows 脚本已提供，当前环境不是 Windows，未执行 Windows 任务计划程序/systemd 部署。

## 公网只读检查结果

检查了 Binance 公共历史、REST、WebSocket 和 GeckoTerminal：

- 成功下载并校验 BTCUSDT 2026-03 的 186 根与 2026-04 的 180 根 4h K 线，共 366 根。
- 其它尝试的归档、部分 REST 和 GeckoTerminal 请求在当前环境超时。REST `/time` 未通过；后续交易规则请求可返回，但这不代表稳定连接。
- 10 秒观察窗口内未获得可确认同步的新鲜 Binance WebSocket 订单簿，**实时流没有通过公网端到端验证**。
- 这次有限实际数据生成 318 条成熟候选训练行，低于默认 CEX 600 行阈值，训练正确返回 `insufficient_mature_labels`。未降低门槛来包装成成功模型。
- 无用户凭证，未向真实 Telegram 发送消息，未调用需 Key 的 Jupiter 报价，也没有验证你未来选择的链上数据供应商。

原始只读检查摘要在 `examples/public_integration.json`。它是接入记录，不是成交记录。请在你的电脑运行 `doctor --online`，使用实际配置后的接口验证连接；本包不会把未成功连接的平台显示成已验证。

## 测试环境

| 组件 | 实际版本 |
|---|---|
| Python | 3.12.14 |
| NumPy | 2.3.5 |
| pandas | 2.2.3 |
| scikit-learn | 1.8.0 |
| PyYAML | 6.0.3 |
| websockets | 16.0 |

依赖声明允许兼容范围，没有声称每一个范围内的版本和操作系统都已测试。若要复现此环境的包版本，可使用 `requirements-tested.txt`；底层系统库/CPU 仍可能不同。

## 未声称完成的事项

没有实盘收益测试、长期前向收益证明、预训练百倍模型、全链失败币完整数据库、MEV/真实队列/链上状态分叉模拟或云端部署。源码中的接口占位已明确给出格式；外部服务开通、Key 和该服务返回字段由你补齐。
